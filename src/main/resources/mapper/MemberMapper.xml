<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="member">
<insert id="save" parameterType="member">
    insert into member_table(memberEmail,memberPassword,memberName,memberMobile,memberAdress,carModel,carYear)
            values (#{memberEmail},#{memberPassword},#{memberName},#{memberMobile},#{memberAdress},#{carModel},#{carYear})
</insert>
<select id="emailCk" parameterType="String" resultType="String">
    select memberEmail from member_table where memberEmail = #{memberEmail}
</select>
<select id="login" parameterType="member" resultType="member">
   select * from member_table where memberEmail=#{memberEmail} and memberPassword =#{memberPassword}
</select>
<select id="detail" parameterType="Long" resultType="member">
    select * from member_table where id=#{id};
</select>
</mapper>














